# Frontend local env
# Local repo values are used first, then bidmart-infrastructure/.env if present

VITE_API_BASE_URL=
VITE_GOOGLE_CLIENT_ID=

# Vercel production:
# Production URL: https://bidmart-frontend.vercel.app
# Set VITE_API_BASE_URL to the public Heroku gateway URL once gateway/infrastructure is ready.
# Set VITE_GOOGLE_CLIENT_ID in Vercel project environment variables.
